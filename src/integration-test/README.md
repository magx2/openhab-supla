# Integration Tests

This directory contains the integration tests for the project.

## Running Integration Tests

To run the integration tests, use the following Maven command:

```bash
mvn verify
```

This will run all tests that end with `IT.java`.
